import{Check,Flag}from'lucide-react';const vals=[100,200,300,600,1000];export default function GoalTimeline({completedDistance,nextMilestone}){return <section className="timeline-card"><p className="eyebrow">Camino al objetivo</p><div className="timeline-list">{vals.map(v=>{const c=v<=completedDistance,n=v===nextMilestone,f=v===1000;return <div className={`timeline-item ${c?'completed':''} ${n?'current':''}`} key={v}><div className="timeline-marker">{c?<Check size={13}/>:f?<Flag size={12}/>:null}</div><strong>{v} km</strong><span>{c?'Completado':n?'Próximo':f?'Objetivo final':''}</span></div>})}</div></section>}
