import{CalendarDays,ChevronRight,Flag}from'lucide-react';export default function MilestoneModule({milestone,onPrepare}){const pending=Math.max(0,milestone.target-milestone.longest);return <section className="milestone-card"><p className="eyebrow">Próximo hito</p><div className="milestone-art"><Flag size={78}/></div><strong>{milestone.target} km</strong><div className="milestone-date"><CalendarDays size={17}/><span>{new Date(`${milestone.date}T00:00:00`).toLocaleDateString('es-CL',{day:'numeric',month:'long',year:'numeric'})}</span></div><p>{pending} km restantes</p><button onClick={onPrepare}>Preparar salida <ChevronRight size={18}/></button></section>}
